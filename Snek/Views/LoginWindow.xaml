<Window x:Class="Snek.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Snek.Views"
        mc:Ignorable="d"
        Icon="/Resources/Icon.ico"
        Title="LoginWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="Аутентификация" TextAlignment="Center" FontSize="20" FontWeight="Bold" MaxWidth="500"/>
        
        <StackPanel Grid.Row="1" Margin="0,10,0,0" VerticalAlignment="Center" MaxWidth="500">
            <TextBlock Text="Логин" />
            <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"/>
            
            <TextBlock Text="Пароль" Margin="0,10,0,0"/>
            <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}"/>
            
            <TextBlock Margin="0,20,0,0" Foreground="Red" Text="{Binding ErrorMessage}" />
        </StackPanel>
        
        
        <StackPanel Grid.Row="2" MaxWidth="500" Margin="0,15,0,0">
            <Button Content="Войти" Command="{Binding SignInCommand}"/>
            <Button Content="Войти как гость" Margin="0,15,0,0" Command="{Binding SignAsGuestCommand}"/>
        </StackPanel>
    </Grid>
</Window>
